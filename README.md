# XRP/USDT Telegram Signals Bot V3.2

بوت إشارات تداول لزوج XRP/USDT يعمل بنمط Paper Trading مع نظام Kill Switch متعدد الطبقات.

## Kill Switch V3.2

### شرط نضج البيانات (Data Maturity Gate)
- لا يُفعّل أي Kill Switch قبل إتمام 5 صفقات مغلقة
- قبل 5 صفقات: يُسمح بالتداول الافتراضي بدون تقييم

### طبقات الحماية (بالترتيب)

1. **Loss Streak Protection** (أعلى أولوية)
   - 3 خسائر متتالية = إيقاف فوري
   - تنبيه: `🛑 تم إيقاف التداول مؤقتًا بسبب 3 خسائر متتالية`

2. **Drawdown Protection**
   - انخفاض الرصيد > 3% من أعلى رصيد (Peak Balance)
   - تنبيه: `🛑 تم إيقاف التداول بسبب تجاوز حد الخسارة الكلية (Drawdown)`

3. **Recent Performance Degradation**
   - Win Rate < 40% في آخر 10 صفقات
   - يُتجاهل إذا عدد الصفقات < 10

### سلوك الإيقاف
- ❌ لا يتم فتح صفقات جديدة
- ✅ الصفقات المفتوحة تستمر حتى الخروج الطبيعي
- 📨 تنبيه واحد فقط مع سبب الإيقاف

### استئناف التداول
- **تلقائي**: بعد 30 دقيقة
- **يدوي**: أمر `/استئناف`
- يتم تصفير سلسلة الخسائر عند الاستئناف

## أوامر تيليجرام

| الأمر | الوصف |
|-------|--------|
| `/الحالة` | حالة التداول وسبب الإيقاف |
| `/احصائيات` | الأداء الكامل |
| `/استئناف` | استئناف يدوي |
| `/start` | رسالة الترحيب |
| `/status` | حالة البوت |
| `/balance` | الرصيد |
| `/trades` | آخر الصفقات |
| `/stats` | الإحصائيات |
| `/diagnostic` | تشخيص تشغيلي شامل للحالة والسوق |
| `/on` | تشغيل الإشارات |
| `/off` | إيقاف الإشارات |
| `/reset` | تصفير الرصيد |
| `/الفريم 1/5` | تغيير الفريم |

## ملف السجل

`paper_trades.csv` يحتوي على:
- `kill_switch_triggered`: True/False
- `kill_switch_reason`: سبب التفعيل
- `balance_peak`: أعلى رصيد

## المتغيرات المطلوبة

| Key | Value |
|-----|-------|
| `TG_TOKEN` | توكن البوت من @BotFather |
| `TG_CHAT_ID` | رقم المحادثة |

## التثبيت

```bash
pip install python-telegram-bot requests
```

## التشغيل

```bash
python main.py
```

## القيود

- ❌ لا Binance API Key
- ❌ لا تنفيذ حقيقي
- ❌ لا Futures
- ❌ لا Short/بيع
- ✅ صفقة واحدة فقط في نفس الوقت
- ✅ قراءة الأسعار فقط

## تحذير

هذا البوت للأغراض التعليمية فقط. لا يمثل نصيحة استثمارية.
