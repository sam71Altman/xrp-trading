📌 وصف المشكلة
حاليًا زر سجل الصفقات يعرض قائمة فرعية بأزرار (عرض السجل / إحصائيات / تصفير).
هذا غير مطلوب.
✅ السلوك المطلوب (دقيق)
الضغط على 📜 سجل الصفقات:
يعرض الصفقات المسجلة مباشرة
بدون أي قائمة
بدون أي أزرار إضافية
أسفل نفس الرسالة فقط يظهر زر واحد:
🗑️ تصفير سجل الصفقات
🚫 قيود صارمة
ممنوع:
❌ إضافة قوائم
❌ إضافة أزرار اختيار
❌ تغيير منطق السجل
❌ تغيير شكل الرسالة
❌ تغيير ترتيب الصفقات
❌ إضافة زر إحصائيات هنا
هذا إصلاح واجهة فقط.
✅ التدفق الصحيح (UX Flow)
نسخ التعليمات البرمجية
Text
[المستخدم]
⬇️ يضغط: 📜 سجل الصفقات

[البوت]
📜 آخر الصفقات المنفذة
1️⃣ صفقة BUY +0.16%
2️⃣ صفقة SELL -0.27%
3️⃣ صفقة BUY +0.02%
...

[زر واحد فقط أسفل الرسالة]
🗑️ تصفير سجل الصفقات
🧠 التنفيذ المطلوب
1️⃣ زر سجل الصفقات (بدون قائمة)
نسخ التعليمات البرمجية
Python
@bot.message_handler(func=lambda m: m.text == "📜 سجل الصفقات")
def show_trade_history(message):
    history_text = render_trade_history()

    keyboard = InlineKeyboardMarkup()
    keyboard.add(
        InlineKeyboardButton(
            text="🗑️ تصفير سجل الصفقات",
            callback_data="CLEAR_TRADE_HISTORY"
        )
    )

    bot.send_message(
        message.chat.id,
        history_text,
        reply_markup=keyboard
    )
❌ ممنوع:
نسخ التعليمات البرمجية
Python
# ❌ لا قوائم
keyboard.add("عرض السجل")
keyboard.add("إحصائيات")
2️⃣ زر التصفير (مع تأكيد)
عند الضغط:
نسخ التعليمات البرمجية
Python
@bot.callback_query_handler(func=lambda c: c.data == "CLEAR_TRADE_HISTORY")
def confirm_clear_history(call):
    keyboard = InlineKeyboardMarkup()
    keyboard.add(
        InlineKeyboardButton("✅ نعم، صفّر السجل", callback_data="CONFIRM_CLEAR_HISTORY"),
        InlineKeyboardButton("❌ إلغاء", callback_data="CANCEL_CLEAR_HISTORY")
    )

    bot.edit_message_reply_markup(
        call.message.chat.id,
        call.message.message_id,
        reply_markup=keyboard
    )
3️⃣ تنفيذ التصفير
نسخ التعليمات البرمجية
Python
@bot.callback_query_handler(func=lambda c: c.data == "CONFIRM_CLEAR_HISTORY")
def clear_history(call):
    clear_trade_history()

    bot.edit_message_text(
        "✅ تم تصفير سجل الصفقات بنجاح.\nابدأ فترة جديدة.",
        call.message.chat.id,
        call.message.message_id
    )
🛡️ ما الذي لا يتأثر
✔ الرصيد
✔ الصفقة المفتوحة
✔ إعدادات البوت
✔ الاستراتيجية
✔ الذكاء
✅ معايير النجاح (اختبار بصري)
الضغط على سجل الصفقات:
لا تظهر قائمة
لا تظهر أزرار متعددة
يظهر:
سجل الصفقات
زر واحد فقط: 🗑️ تصفير سجل الصفقات
نفس شكل الصورة اللي أرسلتها ✔️