إضافة زر داخل قائمة سجل الصفقات يسمح للمستخدم بـ تصفير / مسح جميع الصفقات القديمة (Paper Trading فقط)،
لغرض:
بدء فترة اختبار جديدة
تحليل أداء نسخة جديدة
تنظيف السجل بدون إعادة تشغيل البوت
⚠️ قيود صارمة (غير قابلة للتفاوض)
❌ لا تغيير في منطق الدخول
❌ لا تغيير في منطق الخروج
❌ لا تغيير في الاستراتيجية
❌ لا تغيير في الرصيد الحالي
❌ لا تغيير في الصفقة المفتوحة (إن وجدت)
❌ لا حذف ملفات النظام أو الإعدادات
✅ التأثير على سجل الصفقات فقط
✅ السلوك المطلوب
1️⃣ مكان الزر
الزر يكون داخل قائمة سجل الصفقات
اسمه واضح وصريح
نسخ التعليمات البرمجية
Text
🗑️ تصفير سجل الصفقات
2️⃣ تدفق الاستخدام (UX Flow)
نسخ التعليمات البرمجية
Text
📂 سجل الصفقات
├─ 📊 عرض السجل
├─ 📈 إحصائيات السجل
└─ 🗑️ تصفير سجل الصفقات
3️⃣ إجراء الأمان (تأكيد إلزامي)
عند الضغط على زر التصفير:
نسخ التعليمات البرمجية
Text
⚠️ تأكيد تصفير سجل الصفقات

سيتم حذف:
• جميع الصفقات المغلقة
• جميع بيانات الأداء السابقة

لن يتم حذف:
• الرصيد الحالي
• الصفقة المفتوحة (إن وجدت)
• إعدادات البوت

[✅ نعم، صفّر السجل]
[❌ إلغاء]
❌ لا يتم التصفير بدون هذا التأكيد.
🧠 التنفيذ التقني المطلوب
4️⃣ ما الذي يتم تصفيره بالضبط
نسخ التعليمات البرمجية
Python
# يتم حذف / تصفير:
trade_history = []
closed_trades = []
trade_logs = []
performance_stats = reset()

# لا يتم المساس بـ:
current_balance
open_trade
bot_state
settings
5️⃣ دالة التصفير (مثال إلزامي)
نسخ التعليمات البرمجية
Python
def clear_trade_history():
    """
    تصفير سجل الصفقات فقط
    """
    global trade_history, closed_trades, trade_logs, performance_stats
    
    trade_history.clear()
    closed_trades.clear()
    trade_logs.clear()
    performance_stats.reset()
    
    log("[HISTORY] Trade history cleared by user action")
6️⃣ زر Telegram (Callback)
نسخ التعليمات البرمجية
Python
InlineKeyboardButton(
    text="🗑️ تصفير سجل الصفقات",
    callback_data="CLEAR_TRADE_HISTORY"
)
7️⃣ معالجة الحدث (Handler)
نسخ التعليمات البرمجية
Python
if callback.data == "CLEAR_TRADE_HISTORY":
    show_confirmation_dialog()
وعند التأكيد:
نسخ التعليمات البرمجية
Python
if callback.data == "CONFIRM_CLEAR_HISTORY":
    clear_trade_history()
    send_message("✅ تم تصفير سجل الصفقات بنجاح.\nابدأ فترة اختبار جديدة.")
🧪 شروط السلامة (إلزامية)
إذا كانت هناك صفقة مفتوحة:
⚠️ لا يتم إغلاقها
⚠️ لا يتم تعديلها
التصفير يؤثر فقط على الصفقات المغلقة
العملية قابلة للتكرار بدون مشاكل
لا حاجة لإعادة تشغيل البوت
📊 التحقق بعد التنفيذ
بعد الضغط على الزر:
نسخ التعليمات البرمجية
Text
📂 سجل الصفقات
• عدد الصفقات: 0
• إجمالي الربح: 0.00 USDT
• آخر صفقة: —
🏷️ تسجيل الحدث (Logging)
نسخ التعليمات البرمجية
Python
log_event({
    "action": "CLEAR_TRADE_HISTORY",
    "timestamp": now(),
    "user": telegram_user_id,
    "version": BOT_VERSION
})
✅ معايير النجاح
✔ الزر يظهر داخل قائمة السجل
✔ لا يعمل بدون تأكيد
✔ السجل يُصفّر بالكامل
✔ الرصيد لا يتغير
✔ الصفقة المفتوحة لا تتأثر
✔ لا أخطاء أو Exceptions
✔ العملية واضحة للمستخدم