TP CONTINUATION / PROTECTED RUNNER — FINAL SPEC (PROMPT READY)
🎯 الهدف
تمكين السكالب السريع من استغلال الامتدادات القوية بعد تحقيق الهدف،
دون كسر فلسفة السكالب أو التأثير على الصفقات العادية.
🧭 النطاق
FAST_SCALP_AGGRESSIVE فقط
اختياري بالكامل
افتراضيًا: معطّل
نسخ التعليمات البرمجية

ENABLE_TP_CONTINUATION = False
🧱 المبدأ الأساسي
نسخ التعليمات البرمجية

TP = نقطة قرار
ليس إغلاقًا أعمى
إما:
إغلاق طبيعي (السلوك الحالي)
أو التحول إلى Protected Runner
🔁 تسلسل التنفيذ (دقيق)
1️⃣ عند لمس TP (Tick-level)
❌ لا يتم الإغلاق فورًا
2️⃣ شرط التفعيل (MANDATORY)
يتم التفعيل فقط إذا أُغلقت شمعة كاملة فوق TP:
نسخ التعليمات البرمجية
Python
tp_confirmed = candle.close > TP
❗ يمنع fake breakouts
✂️ 3️⃣ إغلاق جزئي (اختياري لكن موصى به)
نسخ التعليمات البرمجية
Python
PARTIAL_CLOSE_PERCENT = 0.6  # 60%
يُغلق الجزء الأساسي
المتبقي يتحول إلى Runner
إذا = 1.0 → سلوك TP القديم
🛡️ 4️⃣ رفع وقف الخسارة (إجباري)
نسخ التعليمات البرمجية
Python
local_low = lowest(low, 5)
ema_fast_support = EMA(20)

new_sl = max(
    entry_price,
    local_low,
    ema_fast_support
)
📌 بعد هذا:
الصفقة Risk-Free فعلياً
🧲 5️⃣ شروط الاستمرار (ALL REQUIRED)
نسخ التعليمات البرمجية
Python
CONTINUE_IF = (
    RSI > 55 and
    volume >= avg_volume and
    MACD_histogram > 0 and
    price > VWAP and
    not rejection_candle
)
❌ إذا فشل أي شرط → إغلاق فوري
📈 6️⃣ Trailing Logic (متدرج + آمن)
نسخ التعليمات البرمجية
Python
if profit_pct >= 2%:
    trail_sl = entry + 1%
elif profit_pct >= 3%:
    trail_sl = entry + 1.5%
elif profit_pct >= 5%:
    trail_sl = entry + 2%
ATR-based override مسموح
لا يوجد Trailing عدواني (لحماية السكالب)
⏱️ 7️⃣ حد زمني للـ Runner
نسخ التعليمات البرمجية
Python
MAX_RUNNER_TIME = 60 minutes
بعده → إغلاق Market
يمنع تحول السكالب إلى Swing
🚪 8️⃣ خروج ضعف الزخم (Mandatory Escape)
نسخ التعليمات البرمجية
Python
if volume < avg_volume * 0.7 and RSI < 60:
    close_runner(reason="MOMENTUM_FADE")
🧯 9️⃣ أمان مطلق
Touch new_sl → إغلاق فوري Market
لا EMA Exit
لا AI
لا Time Lock
📊 10️⃣ Metrics إلزامية
نسخ التعليمات البرمجية
Python
RUNNER_METRICS = {
    "runner_triggered": count,
    "avg_runner_profit": pct,
    "runner_sl_hits": count,
    "runner_timeouts": count
}
🧾 11️⃣ Logging شفاف
نسخ التعليمات البرمجية

[TP_CONTINUATION] activated
[PARTIAL_CLOSE] 60%
[SL_RAISED] to 1.7482
[RUNNER_EXIT] reason=TRAIL_HIT profit=+1.9%
🚫 محظورات صارمة
❌ لا تعديل Entry
❌ لا تغيير TP الأصلي
❌ لا تأثير على SCALP_PULLBACK / BREAKOUT
❌ لا تغيير Execution Priority
🔄 Fallback (مهم)
إذا:
نسخ التعليمات البرمجية

ENABLE_TP_CONTINUATION = False
➡️ السلوك الحالي يبقى 100% بدون أي فرق