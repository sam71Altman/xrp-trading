📌 وصف المشكلة
بعض رسائل البوت (خصوصًا رسالة التشخيص) ما زالت تعرض إصدارًا قديمًا مثل V3.2
بينما البوت فعليًا يعمل بالإصدار v3.7.1-lite.
السبب:
وجود تعريفات محلية ثابتة للنسخة
وعدم الاعتماد على مصدر موحد واحد للإصدار.
🎯 الهدف النهائي
ضمان أن جميع رسائل البوت بدون استثناء (الحالة، التشخيص، الإشارات، السجلات):
تعرض نفس رقم الإصدار
من مصدر واحد فقط
مع منع رجوع المشكلة مستقبلًا.
⚠️ القيود الصارمة
❌ ليس تحديث استراتيجية
❌ ليس تغيير منطق تداول
❌ ليس تحسين أداء
✅ إصلاح بنية وعرض فقط
✅ توحيد مرجعية النسخة (Single Source of Truth)
✅ المتطلبات الإلزامية
1️⃣ إنشاء مصدر النسخة الموحد (إلزامي)
نسخ التعليمات البرمجية
Python
# 📁 version.py
"""
المصدر الوحيد الموثوق لإصدار البوت
⚠️ ممنوع تعريف رقم النسخة في أي ملف آخر
التنسيق:
vMajor.Minor.Patch[-variant]
مثال: v3.7.1-lite
"""

BOT_VERSION = "v3.7.1-lite"
2️⃣ تعديل وحدة التشخيص (diagnostics)
نسخ التعليمات البرمجية
Python
# 📁 diagnostics.py
from version import BOT_VERSION

async def send_diagnostics():
    """
    إرسال تشخيص البوت باستخدام النسخة الموحدة
    """
    log(f"[DIAGNOSTIC] Using unified bot version: {BOT_VERSION}")
    
    message = f"""
🧪 تشخيص البوت {BOT_VERSION}
━━━━━━━━━━━━━━━━━━━━━
• الإصدار: {BOT_VERSION}
• الحالة: نشط
• الوقت: {get_current_time()}
    """
    
    # ❌ ممنوع:
    # - كتابة "V3.2" أو أي رقم نسخة كنص ثابت
    # - تعريف version داخل الدالة
    # - استخدام config قديم للنسخة
    
    await send_message(message)
3️⃣ تطبيق التوحيد في جميع الوحدات الأخرى
في أي ملف يعرض الإصدار (status / signals / logs):
نسخ التعليمات البرمجية
Python
from version import BOT_VERSION

f"📊 حالة البوت الحالية {BOT_VERSION}"
❌ ممنوع تمامًا:
نسخ التعليمات البرمجية
Python
"V3.2"
"V3.6"
"v3.7"
4️⃣ التحقق عند بدء التشغيل (Startup Validation)
نسخ التعليمات البرمجية
Python
# 📁 main.py
from version import BOT_VERSION
import re

def validate_version_unification():
    """
    تحقق حازم من توحيد النسخة
    """
    pattern = r'^v\d+\.\d+\.\d+(-[a-zA-Z0-9]+)?$'
    
    if not re.match(pattern, BOT_VERSION):
        raise RuntimeError(f"Invalid bot version format: {BOT_VERSION}")
    
    log(f"[SYSTEM] Bot version unified successfully: {BOT_VERSION}")
يتم استدعاؤها مرة واحدة عند الإقلاع.
5️⃣ فحص تحذيري لتعريفات النسخ المحلية (اختياري – غير مكسِر)
نسخ التعليمات البرمجية
Python
def check_local_version_definitions():
    """
    تحذير فقط – لا يوقف التشغيل
    """
    suspicious_tokens = ["V3.", "v3."]
    log("[VERSION CHECK] Scanning for hardcoded version strings...")
    # الهدف: تنبيه المطور فقط
6️⃣ اختبار آلي لمنع رجوع المشكلة (إلزامي)
نسخ التعليمات البرمجية
Python
# 📁 tests/test_version_unity.py
from version import BOT_VERSION

async def test_version_unification():
    from diagnostics import send_diagnostics
    from status import send_status
    
    diag = await send_diagnostics(test_mode=True)
    stat = await send_status(test_mode=True)
    
    assert BOT_VERSION in diag
    assert BOT_VERSION in stat
    
    print(f"✅ All modules use unified version: {BOT_VERSION}")
📊 النتيجة المتوقعة بعد التطبيق
نسخ التعليمات البرمجية

🧪 تشخيص البوت v3.7.1-lite
📊 حالة البوت v3.7.1-lite
🚀 إشارة شراء v3.7.1-lite
[SYSTEM] Bot version unified successfully: v3.7.1-lite
✅ معايير النجاح
✔ لا يوجد أي رقم نسخة كنص ثابت
✔ جميع الرسائل تستورد من version.py
✔ التشخيص لا يعرض V3.2 مرة أخرى
✔ الاختبار الآلي يمر بنجاح
✔ تحديث النسخة مستقبلًا يتم من ملف واحد فقط
🧠 قاعدة ذهبية (مهمة)
أي رسالة لا تستورد BOT_VERSION من version.py تعتبر Bug.