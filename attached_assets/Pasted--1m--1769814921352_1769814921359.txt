أريد إعادة بناء نظام فلترة ذكاء اصطناعي لبوت تداول سكالب 1m بطريقة هندسية نظيفة وجاهزة للإنتاج.

⚠️ مهم جداً:
التصميم يجب أن يكون بسيط + آمن + واضح.
لا أريد أي حلول Hack أو monkey patch أو استبدال دوال ديناميكياً.
أريد Architecture احترافي فقط.

========================================
🎯 الهدف العام
========================================

دمج فلتر ذكاء اصطناعي حقيقي يتحكم في السماح/منع الصفقات قبل التنفيذ.

قاعدة الدخول:
AI_score >= AI_weight → يسمح
AI_score < AI_weight → يمنع

========================================
🔧 المتطلبات الإلزامية (Must Have)
========================================

1) AI Weight حقيقي:
قيم فقط:
0.0 = OFF
0.3 = LOW
0.6 = MEDIUM
1.0 = HIGH

2) أوضاع الذكاء:
OFF  → لا فلترة
LEARN → تحليل فقط بدون منع
FULL → فلترة فعلية

3) حساب score بسيط (لا ML ولا تعقيد):
فلتر سكالب يعتمد فقط على:
- Volume
- ATR
- Trend strength
- RSI
- Spread

score بين 0.0 و 1.0

4) السلامة أولاً:
إذا حدث أي خطأ في الحساب:
لا ترجع 0.5
بل:
return None

وفي منطق الدخول:
score is None → BLOCK_SYSTEM_ERROR

5) السقف اليومي (Impact Cap):
يحسب فقط "التدخلات" وليس كل الصفقات.

التدخل = عندما يمنع الذكاء صفقة.

if score < weight:
    interventions += 1

وعند وصول الحد:
يتوقف الذكاء مؤقتاً ويُسمح بكل الصفقات (fallback).

6) كولدوان بين الصفقات:
trade_cooldown = 30 ثانية لكل زوج
لا يسمح بدخول صفقة جديدة لنفس الزوج قبل انتهاء المدة.

7) بدون Global State خطير:
لا تستخدم Singleton.
لا تستخدم متغيرات عامة مشتركة.

استخدم:
class AIState داخل TradingEngine instance فقط.

كل Engine له state مستقل.

8) بدون Monkey Patch:
ممنوع:
setattr
تبديل دوال وقت التشغيل
حيل التفاف

بدلاً من ذلك:
Dependency Injection فقط.

يعني:
TradingEngine(check_entry, execute_trade)

9) مسار دخول واحد فقط:
ممنوع استدعاء execute_trade مباشرة.

التصميم يجب أن يكون:

check_and_execute_trade()
    ↓
AI filter
    ↓
original conditions
    ↓
execute_trade()

ولا يوجد أي مسار آخر.

10) Logging واضح:
لكل صفقة:
[AI] SYMBOL | score=0.72 | weight=0.6 | ALLOWED
أو
BLOCKED

========================================
🏗️ المطلوب في الكود
========================================

أريد ملفات منظمة:

ai_state.py
- AIState
- counters
- cooldowns
- limits

ai_filter.py
- SimpleAIFilter
- calculate_score()

trading_engine.py
- TradingEngine
- check_and_execute_trade()

telegram_interface.py
- تغيير:
mode
weight
impact_level
- عرض الحالة

monitor.py
- get_status()
- validate_system()

========================================
🎯 قواعد التصميم
========================================

- الكود بسيط وواضح
- أقل عدد أسطر ممكن
- لا تعقيد غير ضروري
- لا async
- لا hacks
- قابل للصيانة بسهولة
- Production ready

========================================
📊 السلوك المطلوب نهائياً
========================================

OFF → كل الصفقات تمر
LEARN → تحليل فقط + logs
FULL → فلترة حقيقية

عند خطأ → منع الصفقة
عند cooldown → منع
عند score ضعيف → منع
عند تجاوز السقف → السماح للجميع

========================================
🎯 الناتج المتوقع
========================================

نظام:
- مستقر
- آمن
- deterministic
- لا يتصرف بشكل غامض
- كل زر في التيليجرام يغير السلوك فعلياً

اكتب الكود كاملاً جاهز للإنتاج مع تعليقات مختصرة فقط.
لا أريد شرح، فقط تنفيذ نظيف.